# Database Layer (GoldenWattle)

- `tables/`: DDL for ODS (raw) and FTR (indicator) tables.
- `views/`: Helper views, such as `VW_MISSING_SYMBOLS`.
- `procedures/`: PL/SQL engines for EMA, PSAR, etc.
