# Source Code (The Ascent)

- `main.py`: Orchestrator for all ingestion tasks.
- `db_operator.py`: Singleton DB manager with chunked merge logic.
- `config.py`: Environment loader and validator.
- `scrapers/`: Modules for Yahoo, AFR, Shortman, etc.
